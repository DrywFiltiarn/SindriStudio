# P1-E1 Report — JobQueue + VramLedger

**Task:** anvilml-scheduler: JobQueue + VramLedger  
**Phase:** 1  
**Status:** ✅ COMPLETE — Committed & Pushed

## What Was Implemented

### `queue.rs` — JobQueue
- `JobQueue` struct backed by `VecDeque<Job>` with FIFO ordering
- Methods: `new()`, `push()`, `pop_front()`, `cancel()`, `get()`, `list()`, `len()`, `is_empty()`
- 6 unit tests covering all operations

### `ledger.rs` — VramLedger
- `VramLedger` struct with HashMap-based per-device tracking
- Methods: `new()`, `set_budget()`, `used_mib()`, `free_mib()`, `would_fit()`, `mark_loaded()`, `mark_evicted()`
- 6 unit tests covering all operations

### `lib.rs` — Module declarations added
### `Cargo.toml` — Added `chrono` and `uuid` workspace dependencies

## Test Results
```
running 13 tests
test ledger::tests::test_mark_evicted ... ok
test ledger::tests::test_free_mib ... ok
test ledger::tests::test_used_mib_sums ... ok
test ledger::tests::test_multiple_devices ... ok
test ledger::tests::test_would_fit_over_budget ... ok
test ledger::tests::test_would_fit_under_budget ... ok
test queue::tests::test_cancel_removes_job ... ok
test queue::tests::test_cancel_unknown_id ... ok
test queue::tests::test_get_by_id ... ok
test queue::tests::test_len ... ok
test queue::tests::test_pop_front_empty ... ok
test queue::tests::test_push_pop_front ... ok
test tests::it_works ... ok

test result: ok. 13 passed; 0 failed
```

**Acceptance criteria met:** `cargo test -p anvilml-scheduler` exits 0 with 13 tests (≥8 minimum).

## Files Changed
| File | Action |
|------|--------|
| `crates/anvilml-scheduler/src/queue.rs` | Created — JobQueue + 6 tests |
| `crates/anvilml-scheduler/src/ledger.rs` | Created — VramLedger + 6 tests |
| `crates/anvilml-scheduler/src/lib.rs` | Modified — added module declarations |
| `crates/anvilml-scheduler/Cargo.toml` | Modified — added chrono, uuid deps |
| `Cargo.lock` | Regenerated |

## Commit
- Hash: `1d9430e`
- Message: `P1-E1: anvilml-scheduler — JobQueue + VramLedger (13 tests)`
- Pushed to `origin/main`
